FROM fedora:latest
LABEL xyz.akdev.run="podman run \
    --rm \
    --security-opt label=disable \
    -it \
    -v \$PWD:\$PWD \
    -w \$PWD \
    \$IMAGE '$@'"

RUN dnf install -y \
    ripgrep

ENTRYPOINT ["rg"]
