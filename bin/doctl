#!/bin/bash

exec podman run --rm -it \
    --security-opt label=disable \
    --pid=host \
    -e DIGITALOCEAN_ACCESS_TOKEN \
    -v "$PWD:$PWD" \
    -w "$PWD" \
    ghcr.io/akdev1l/container-images/doctl:latest "$@"
