#!/bin/bash

exec podman run --rm -it \
    --security-opt label=disable \
    -p 4000:4000 \
    -v "$PWD:$PWD" \
    -w "$PWD" \
    --entrypoint bundle \
    ghcr.io/akdev1l/container-images/jekyll:latest "$@"
